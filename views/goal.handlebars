<div class="text-center">
  <h2>{{goal.name}}</h2>
  {{!-- <p>{{{get_emoji}}}</p> --}}
  <p>{{goal.description}}</p>
  <p>Created by {{goal.user.name}} on {{goal.date_created}}</p>
  <p>
    {{!-- <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed --}}
  </p>
  <p>
    <button class="btn btn-primary">Reach this goal!</button>
  </p>
</div>